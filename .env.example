# نسخ إلى .env (لا ترفع .env إلى Git)
#
# === Neon (PostgreSQL سحابي، مناسب بدون Docker) ===
# 1) https://console.neon.app — أنشئ مشروعاً (Free)
# 2) Branch → Connection details → انسخ Connection string
#    إن وُجد خياران: للهجرات Prisma استخدم رابط "direct" إن فشل migrate مع الـ pooler
# 3) الصقه في DATABASE_URL (غالباً أضف ?sslmode=require إن لم يكن في الرابط)
#
# DATABASE_URL="postgresql://USER:PASSWORD@ep-xxxx.neon.tech/neondb?sslmode=require"
#
# === أو محلياً مع Docker ===
# docker compose up -d
# DATABASE_URL="postgresql://amarati:amarati@localhost:5432/amarati?schema=public"

DATABASE_URL="postgresql://USER:PASSWORD@ep-xxx.neon.tech/neondb?sslmode=require"

AUTH_SECRET="generate-a-long-random-string-at-least-16-chars"

# بريد (أو أكثر مفصولة بفاصلة) لمراجعة طلبات شركات الصيانة
PLATFORM_ADMIN_EMAILS="admin@example.com"

# رابط الموقع العلني (لروابط الدعوة)
NEXT_PUBLIC_APP_URL="http://localhost:3000"

# Netlify: Site settings → Environment variables
#   - نفس DATABASE_URL (PostgreSQL)
#   - AUTH_SECRET
#   - فعّل النطاق لـ "Builds" و"Functions" (أو الكل) لكل متغير
