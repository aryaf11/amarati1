# دليل مشروع عمارتي (Amarati) — شرح شامل بالعربية

هذا الملف يشرح **فكرة التطبيق**، **التقنيات**، **ربط قاعدة البيانات**، **طريقة العمل داخل الكود**، و**دور الملفات والمجلدات الرئيسية**.  
اسم الحزمة في `package.json`: `amarati1` — **واجهة ويب** (Next.js) لتسجيل السكن وإدارة المبنى، مع **مجلّد تطبيق موبايل اختياري** بلغة **Dart** مع إطار **Flutter** تحت **`amarati_flutter/`**.


---

## 2. التقنيات والأدوات الأساسية

| التقنية | الاستخدام في المشروع |
|--------|----------------------|
| **Node.js** ≥ 20.9 | تشغيل Next و Prisma و السكربتات |
| **Next.js 16** (App Router) | صفحات `src/app/`، تخطيطات، API Routes، Server Components |
| **React 19** | مكوّنات الواجهة |
| **TypeScript** | كل كود `src/` |
| **Tailwind CSS v4** (`@tailwindcss/postcss`) | تنسيق الواجهات |
| **Prisma 5** | ORM ومخطط قاعدة البيانات وهجرات PostgreSQL |
| **PostgreSQL** | قاعدة البيانات الرئيسية |
| **bcryptjs** | تشفير كلمات المرور |
| **jose** | جلسات مبنية على تعمية/توقيع (مع `AUTH_SECRET`) |
| **Zod** | التحقق من المدخلات في Server Actions |

**نشر شائع:**

- **Vercel** — `vercel.json` يحدد أمر بناء يشمل `prisma migrate deploy` وسكربتات أعمدة احتياطية ثم `next build`.
- **Netlify** — دعم جزئي عبر `scripts/netlify-build.cjs` و `devDependencies` المناسبة.

**تكاملات اختيارية (متغيرات بيئة في `.env.example`):**

- Supabase/Vercel Postgres عبر `DATABASE_URL` و `DIRECT_URL`
- **MAINTENANCE_ML_API_URL** لخادم Python (`scripts/maintenance_ml_server.py`) لتقديرات الصيانة

**عميل موبايل (Dart / Flutter) — **`amarati_flutter/`**:

- مشروع مستقل داخل المستودع؛ **البرمجة بلغة Dart** عبر إطار **Flutter**؛ الهدف عموماً تجربة موبايل مكملة لنفس المنصّة أو نسخة عرض لمشروع تخرّج.
- لا يشارك هذا المجلّد في أمر **`npm run build`** لموقع Next على Vercel؛ يُبنّى وتُشغَّل بتثبيت [Flutter SDK](https://flutter.dev) ثم من داخل **`amarati_flutter`**: **`flutter pub get`** ثم **`flutter run`** (أو `flutter build apk`/`ipa` بحسب المنصّة).
- التكامل مع الخادم (عناوين API، مصادقة) يُحدَّد في **`lib/services/api_client.dart`** وحسب المتغيرات أو الثوابت في المشروع.

---

## 3. الربط مع قاعدة البيانات

- **الملف المرجعي**: `prisma/schema.prisma`
- **الاتصال السريع للتطبيق**: `DATABASE_URL` (غالباً مع **connection pooler** مثل Supabase PgBouncer).
- **الاتصال المباشر للهجرات**: `DIRECT_URL` (مضيف قاعدة الفعلي؛ **لا** يُستعمل pooler الذي يمنع قفل الهجرة).
- **في الكود**: ملف `src/lib/prisma.ts` يصدّر عميل Prisma مفرد (يُفضّل لتفادي اتصالات زائدة على serverless).



مجلد **`prisma/migrations/`** يحتوي ملفات SQL تاريخية لكل تغيير في المخطط — لا تُحرّر يدوياً إلا بحذر؛ التعديل المعتاد عبر `schema.prisma` ثم أمر هجرة.

---

## 4. نماذج البيانات الرئيسية (باختصار)

| النموذج | الدور |
|---------|--------|
| `User` | مستخدم: اسم، كلمة مرور، **جوال إلزامي وفريد**، بريد اختياري؛ بعد تأكيد الرمز SMS يُسجَّل التحقق في **`phoneVerifiedAt`**؛ لا يوجد في المنتج حقل أو مسار **صورة شخصية** |
| `Building` | عمارة: اسم، عنوان، حقول عنوان وطني، `inviteCode` فريد، مُنشِئ |
| `Unit` | وحدة (شقة/وحدة ضمن مبنى)؛ تسمية فريدة داخل المبنى |
| `Membership` | ربط مستخدم ↔ وحدة؛ نوع `OWNER`/`TENANT`، وعلم `isSupervisor` |
| `InviteToken` | روابط دعوة مؤقتة لانضمام مستخدم لوحدة معيّنة |
| `Vote`, `VoteOption`, `VoteBallot` | تصويت **مشرف المبنى** (`SUPERVISOR`) وتصويت **شركة صيانة للأعطال المجتمعية** (`MAINTENANCE_COMPANY`)؛ الأخير يُنشأ تلقائياً غالباً عند إنشاء طلب مجتمعي ترافقه ترشيحات شركة، أو يُفتح يدوياً من صفحة الصيانة (منطق **`governance.ts`**). |
| `MaintenanceRequest` | طلب صيانة؛ نطاق شخصي/جماعي؛ تحليل نصّي و **`aiCompaniesJson`** لقوائم شركات؛ الطلب المجتمعي يربط اختيارياً بتصويت شركة واحد (**`Vote.maintenanceRequestId`**). |
| `ApartmentHistoryEvent` | أحداث ظهورها في «جواز الوحدة» |
| `Announcement` | إعلان داخل المبنى |
| `ChatMessage` | رسالة في شات جماعي المبنى |
| `Payment` | نموذج قديم/احتياطي في المخطط؛ **ليست من وظائف واجهة التطبيق** الحالية (لا اعتماد وظيفي في المسارات الرئيسية) |
| `BuildingHealthScore` | سجل درجة صحة شهرية للمبنى |
| `PredictiveMaintenanceAlert` | جدول/سياق مخطّط؛ عرض المنفصل كـ «alert» للصيانة التنبؤية **ليست جزء تجربة المنتج المعتمدة** حالياً |

---

## 5. المصادقة والجلسة

- **`src/lib/session.ts`**: قراءة/كتابة تعريف مستخدم الجلسة في **Cookie** مشفّرة/موقّعة باستخدام `AUTH_SECRET`.
- **`src/lib/current-user.ts`**: `getCurrentUser()` يجلب المستخدم من الجلسة والقاعدة (مع `cache` لطلب واحد).
- **`src/actions/auth.ts`**: تسجيل دخول (مع إجبار تأكيد الجوال عبر OTP إن لم يُثبَّت `phoneVerifiedAt`)، خروج، تحديث ملف شخصي، إصدار OTP، التحقق من الرمز، إعادة الإرسال — يستفيد من **Twilio Verify** أو **رمز مخزَّن محلياً** حسب المتغيرات (راجع `src/lib/twilio-verify.ts` و `sms-otp.ts`).
- **`src/actions/signup.ts`**: إنشاء المستخدم بحقل **`phoneVerifiedAt` فارغ** حتى تأكيد الرمز؛ تهيئة **`emailVerifiedAt`** وفق المنطق الموجود في الكود (`signup.ts`) عندما يكون البريد فارغاً أو مملوءاً؛ إنشاء البنية (مبنى/عضوية أو انضمام)؛ استدعاء **`issuePhoneOtpForUser`**؛ ثم **`createSession`**؛ ثم **`redirect`** إلى **`/register/verify-phone`** مع وسائط **`phone`** و **`next`** (عادة مسار **`/building/{id}`**) حتى يدخل الرمز ويُثبَّت الجوال.

صفحة **`/register/verify-phone`**: نموذج تأكيد الرمز وزر إعادة الإرسال، مع حقل مخفي **`next`** للعودة بعد النجاح.

لا يوجد `middleware.ts` في الجذر؛ الحماية عبر فحص `getCurrentUser()` داخل الصفحات والإجراءات و`redirect` إلى `/login`.

---

## 6. هيكل `src/app/` (مسارات Next.js)

جميع المسارات أدناه تحت **App Router**.

| المسار أو الملف | الوظيفة |
|-----------------|---------|
| `layout.tsx` | تخطيط الجذر؛ لغة وتوجيه وأنماط عامة، تحميل بيئة الخادم |
| `page.tsx` | الصفحة الرئيسية (هبوط) |
| `login/page.tsx` | تسجيل الدخول |
| `signup/page.tsx` | اختيار: إنشاء مبنى / انضمام |
| `signup/create/page.tsx` | نموذج إنشاء مبنى + عنوان وطني |
| `signup/join/page.tsx` | نموذج انضمام برمز مبنى + وحدة |
| `dashboard/page.tsx` | لوحة حساب المستخدم ومبانيَه |
| `dashboard/new/page.tsx` | إعادة توجيه مختصرة |
| `profile/page.tsx` | الملف الشخصي والجواز وبطاقات الإعدادات |
| `register/page.tsx` | إعادة توجيه مسار `/register` نحو الانضمام |
| `register/check-email/page.tsx` | مسار تشغيليلي (قد يكون إعادة توجيه بحسب الإصدار) |
| `register/verify-phone/page.tsx` | التحقق من الجوال OTP بعد التسجيل أو عند الطلب؛ نموذج رمز، إعادة إرسال، والتوجيه إلى **`next`** بعد النجاح |
| `chatbot/page.tsx` | صفحة شات بوت الاستخدام العام |
| `join/[token]/page.tsx` | قبول دعوة انضمام برمز |
| `api/health/route.ts` | فحص صحة التطبيق |
| `api/health/db/route.ts` | فحص اتصال القاعدة |
| `api/maintenance/analyze/route.ts` | نقطة لتحليل صيانة (إن وُجدت) |
| `api/ml/health/route.ts` | فحص خدمة ML الخارجية |
| `building/[buildingId]/layout.tsx` | إطار المبنى + شريط تنقل سفلي/علوي |
| `building/[buildingId]/page.tsx` | الرئيسية داخل المبنى (اختصارات ولوحات) |
| `building/[buildingId]/chat/page.tsx` | شات الجماعة وقائمة السكان |
| `building/[buildingId]/maintenance/page.tsx` | طلبات الصيانة والنماذج |
| `building/[buildingId]/votes/page.tsx` | التصويتات |
| `building/[buildingId]/announcements/page.tsx` | الإعلانات |
| `building/[buildingId]/passport/page.tsx` | جواز الوحدة |
| `building/[buildingId]/invite/page.tsx` | مشاركة رمز/دعوات |
| `building/[buildingId]/supervisor/page.tsx` | لوحة المشرف والدرجات |
| `building/[buildingId]/assistant/page.tsx` | المساعد الذكي داخل المبنى |

---

## 7. مجلد `src/actions/` — إجراءات الخادم (Server Actions)

ملفات تُستدعى من النماذج أو المكوّنات بـ `"use server"`:

| الملف | المحتوى النموذجي |
|-------|------------------|
| `auth.ts` | دخول، خروج، تحديث ملف شخصي، إصدار/التحقق من OTP بالجوال وإعادة الإرسال |
| `signup.ts` | `signupAndCreateBuildingAction`، `signupAndJoinBuildingAction` — إنشاء جلسة ثم توجيه لمسار تأكيد الجوال |
| `social.ts` | إرسال رسالة شات، إدارة إعلانات، إلخ |
| `building.ts` | إنشاء مبنى، ربط وحدة، عمليات إدارية للمبنى |
| `maintenance.ts` | إنشاء طلبات الصيانة وتحليل AI وحفظ **`aiCompaniesJson`**؛ الطلب المجتمعي يطلق **`ensureCommunityMaintenanceCompanyVote`** (من **`governance.ts`**) لفتح **`Vote`** تلقائياً إن وُجدت ترشيحات |
| `votes.ts` | إنشاء تصويتات، إدلاء بصوت، إغلاق تصويت |
| `governance.ts` | تصويت المشرف وشركة الصيانة، إنشاء **`Vote`** مرتبط بطلب مجتمعي، الإغلاق وتطبيق الفائز / تدوين نتيجة الشركة في الطلب |
| `invites.ts` | إنشاء وقبول دعوات |
| `chatbot.ts` | ردود الشات بوت القائمة على قواعد/نصوص |
| `platform.ts` | عمليات أدمن المنصة (إن وُجدت) |

---

## 8. مجلد `src/lib/` — مكتبات مساعدة

| الملف | الغرض |
|-------|--------|
| `prisma.ts` | عميل Prisma |
| `session.ts` | جلسة المستخدم |
| `current-user.ts` | جلب المستخدم الحالي |
| `locale.ts` | لغة الواجهة `ar` / `en` |
| `ui-strings.ts` | كل نصوص الواجهة ثنائية اللغة |
| `access.ts` | صلاحيات العضوية وقوائم المباني |
| `building-context.ts` | تحميل سياق مبنى + عضوية |
| `password.ts` | تجزئة والتحقق من كلمة المرور |
| `action-error-hints.ts` | رسائل أخطاء موحّدة للإجراءات |
| `nav-dict.ts` | تسميات التنقل |
| `chat-types.ts` | أنواع TypeScript لسطور الشات |
| `tokens.ts` | توليد/التحقق من رموز دعوة أو أمان |
| `theme-bootstrap.ts` | ألوان/ثيم أولي |
| `server-env-bootstrap.ts` | نسخ متغيرات بيئة (مثل Vercel Postgres) إلى أسماء Prisma |
| `twilio-verify.ts` | تكامل Twilio Verify (إن مُفعّلاً) |
| `twilio-sms.ts` | SMS عبر Twilio |
| `whatsapp-otp.ts` | رسائل واتساب للـ OTP |
| `sms-otp.ts` | ترتيب قنوات إرسال الرمز |
| `ai-maintenance.ts` | تكامل تحليل نص صيانة |
| `ml-inference.ts` | استدعاء API نموذج ML |
| `maintenance-predictor.ts` | منطق تنبؤ/اقتراح |
| `building-score-band.ts` | نطاقات الدرجة الصحية |
| `strip-markdown.ts` | تنظيف markdown من نصوص |

---

## 9. مجلد `src/components/` — مكوّنات واجهة

| المكوّن أو المجلد | الدور |
|-------------------|--------|
| `ui.tsx` | أزرار، بطاقات، حقول إدخال، هيكل صفحة موحّد |
| `TopNav.tsx` / `BottomNav.tsx` / `NavToolbar.tsx` | شريط علوي/سفلي وتنقل |
| `SubmitButton.tsx` | زر إرسال مع حالة «جاري…» |
| `PasswordInput.tsx` | حقل كلمة مرور مع إظهار/إخفاء |
| `NationalAddressFields.tsx` | حقول العنوان الوطني السعودي |
| `BuildingQuickWidgets.tsx` | اختصارات صفحة المبنى |
| `LandingIcons.tsx` / `LandingLangSwitcher.tsx` | أيقونات ومحوّل لغة الصفحة الرئيسية |
| `ProfileSettings.tsx` | مظهر وإشعارات محلية |
| `MonthlyScoreLog.tsx` | عرض سجل الدرجات الشهرية |
| `JoinAcceptButton.tsx` | قبول دعوة |
| `BackButton.tsx` | زر رجوع في المتصفح |
| `ChatbotClient.tsx` | واجهة شات بوت الاستخدام |
| `assistant/AssistantClient.tsx` | واجهة المساعد داخل المبنى |
| `conversations/*` | تبويبات شات، فقاعات، قائمة سكان، نموذج إرسال |
| `messaging/MessageInputBar.tsx` | شريط إدخال الرسالة |

---

## 10. الملفات في جذر المشروع

| الملف | الغرض |
|-------|--------|
| `package.json` | الاعتماديات والسكربتات |
| `package-lock.json` | قفل إصدارات npm |
| `tsconfig.json` | إعدادات TypeScript ومسارات `@/` |
| `next.config.ts` | إعدادات Next (مثل `allowedDevOrigins`) |
| `eslint.config.mjs` | قواعد ESLint |
| `postcss.config.mjs` | PostCSS + Tailwind |
| `vercel.json` | أمر التثبيت والبناء والمنطقة `sin1` |
| `.env.example` | توثيق المتغيرات المطلوبة والاختيارية |
| `README.md` | توثيق create-next-app الافتراضي (لا يبديل هذا الدليل) |

---

## 11. مجلد `public/`

موارد ثابتة تعرض مباشرة من الجذر: `logo.svg`، بعض SVG الافتراضية، **`manifest.webmanifest`** لاختصارات PWA بسيطة إن استُعملت.

---

## 12. مجلد `scripts/`

أدوات صيانة وبناء خارج التطبيق:

| السكربت | الغرض |
|---------|--------|
| `ensure-building-columns.mjs` / `ensure-user-columns.mjs` | تأكيد أعمدة ناقصة في الإنتاج بعد النشر |
| `prepare-prod-migrate.mjs` | تجهيز هجرات الإنتاج |
| `supabase-direct-url.mjs` | مساعدة روابط Supabase |
| `fix-duplicate-phones.mjs` | إصلاح جوالات مكررة في البيانات |
| `check-prod-db.mjs` / `test-prod-login.mjs` / `prod-db-host.mjs` | فحوص تشغيل/اتصال |
| `push-twilio-env-to-vercel.ps1` | رفع متغيرات Twilio إلى Vercel |
| `complete-manual-setup.ps1` | إعداد يدوي للمطورين على Windows |
| `run-dev-with-ml.ps1` / `dev:ml` | تشغيل التطبيق مع خادم ML |
| `maintenance_ml_server.py` / `requirements-ml.txt` | خادم نموذج صيانة |
| `netlify-build.cjs` | مسار بناء Netlify |

---

## 13. ملاحظات سلوكية مهمة للعميل أو الفريق

1. **الشات الجماعي**: ليس تحديثاً فورياً دفعياً؛ الإرسال عبر **Server Action** ثم `revalidatePath` و`redirect` — المستخدم الآخر لا يرى الرسائل الجديدة حتى يُحدِّث الصفحة أو يُرسل هو رسالة أو يُعيد التحميل.
2. **HTMX**: غير مستخدم في المستودع.
3. **الجوال وبريد وOTP**: الجوال حقول أساسية في التسجيل؛ تأكيده عبر OTP بعد إنشاء الحساب؛ تغيير رقم الجوال من الملف الشخصي يعيد طلب OTP. البريد اختياري؛ حقول **`emailVerifiedAt`** قد تُحدَّث وفق المنطق الحالي بدون تجربة «تحقق بريدي» مطوّلة في كل الحالات (راجع **`auth`** ونماذج التسجيل).
4. **مخطط Prisma ومزود Supabase**: قد لا تكون كل الكيانات الظاهرة في **`schema.prisma`** (مثل **`Payment`** أو أنواع تصويت قديمة) موافقة لمخزَّن أو خدمات Supabase فعلاً؛ المصدر المعتمد للمنتج المنشور هو **الكود** ومسارات الواجهة، لا مجرد قائمة النماذج.
5. **الدفع (`Payment`)** في مخطّط Prisma وليس جزء تجربة واجهة المنتج كما هو موضّح أعلاه؛ **تصويت شركة الصيانة** مفعَّل ضمن بلاغات الصيانة المجتمعية.
6. **الأمان**: لا ترفع ملف `.env` الحقيقي إلى Git؛ استخدم `.env.example` كمرجع فقط.

---


