{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "installCommand": "npm install",
  "buildCommand": "prisma generate && prisma migrate deploy && node scripts/ensure-building-columns.mjs && node scripts/ensure-user-columns.mjs && next build",
  "regions": ["sin1"]
}
